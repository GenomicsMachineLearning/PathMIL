[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pathmil"
version = "0.2.0"
description = "PathMIL: attention-based Multiple Instance Learning for predicting gene/module expression from Visium H&E histology"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [{ name = "Xiao Tan" }]
keywords = ["spatial-transcriptomics", "visium", "histology", "multiple-instance-learning", "deep-learning"]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Intended Audience :: Science/Research",
    "Topic :: Scientific/Engineering :: Bio-Informatics",
]
dependencies = [
    "numpy",
    "pandas",
    "scipy",
    "h5py",
    "pyyaml",
    "tqdm",
    "matplotlib",
    "scikit-learn",
    "torch",
    "anndata",
    "scanpy",
]

# Heavy extras for the `preprocess` (embedding) step. Install with
# `pip install -e .[embed]` only if you need to generate patch embeddings;
# train/predict/plot need just the core dependencies above.
[project.optional-dependencies]
embed = [
    "sopa[stardist,wsi]",
    "spatialdata",
    "spatialdata-io",
    "spatialdata-plot",
    "timm",
    "geopandas",
    "shapely",
    "dask",
    "huggingface_hub",
    "Pillow",
]

# H&E-only inference (`scripts/predict_he.py`): apply a trained checkpoint to a slide.
# Much lighter than [embed] — no sopa/spatialdata, since there are no Visium spots to
# read. Note that `openslide-python` also needs the OpenSlide C library present:
#   conda install -c conda-forge openslide-python   # or: apt install libopenslide0
he = [
    "timm",
    "huggingface_hub",
    "Pillow",
    "openslide-python",
    "tifffile",
    # --sc_pred only: StarDist nucleus segmentation + per-cell geometry output.
    "stardist",
    "csbdeep",
    "tensorflow",
    "shapely",
    "geopandas",
    "pyarrow",
]

# Test suite: `pip install -e .[dev] && pytest`. Core deps only — no GPU, no data.
dev = ["pytest"]

[project.urls]
Homepage = "https://github.com/GenomicsMachineLearning/PathMIL"
Repository = "https://github.com/GenomicsMachineLearning/PathMIL"

[project.scripts]
pathmil = "pathmil.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
# Put this checkout ahead of any installed `pathmil` on sys.path, so the tests
# exercise this tree and not a site-packages (or editable) copy elsewhere.
pythonpath = ["."]

[tool.setuptools]
packages = ["pathmil"]

[tool.setuptools.package-data]
pathmil = ["../configs/*.yaml"]
